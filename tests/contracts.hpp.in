#pragma once
#include <eosio/testing/tester.hpp>

namespace eosio::testing {

struct contracts {
   static std::vector<uint8_t> grabcisum_wasm() { return read_wasm("${CMAKE_BINARY_DIR}/contracts/grab.cisum/grab.cisum.wasm"); }
   static std::vector<char>    grabcisum_abi()  { return read_abi("${CMAKE_BINARY_DIR}/contracts/grab.cisum/grab.cisum.abi"); }
};

} // namespace eosio::testing
